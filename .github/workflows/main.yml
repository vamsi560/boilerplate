name: Terraform Validation

on:
  push:
    branches:
      - main

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v2

      - name: Install Terraform
        uses: hashicorp/setup-terraform@v1

      - name: Run Terraform Validation
        run: |
          chmod +x validate_tf.sh
          ./validate_tf.sh
      
      - name: Send Validation Results
        run: |
          echo "Zipping corrected files"
          zip -r corrected_tf_files.zip ./*.tf
          
          echo "Sending email to approver..."
          echo "Validation results attached." | mail -s "Terraform Validation Results" -A "corrected_tf_files.zip" approver@example.com
